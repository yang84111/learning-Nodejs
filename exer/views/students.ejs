<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        caption{
            font: 25px bold;
        }
        table{
            border-collapse: collapse;
        }
        th,td{
            font: 20px;
            border: 1px solid black;
            padding: 10px;
            text-align: center;
        }
    </style>
</head>
<body>
    <%if(stus && stus.length > 0){%>
    <table>
        <caption>学生列表</caption>
        <thead>
            <tr>
                <th>学号</th>
                <th>姓名</th>
                <th>性别</th>
                <th>年龄</th>
                <th>地址</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            <%for(stu of stus){%>
            <tr>
                <td><%=stu.id %></td>
                <td><%=stu.name %></td>
                <td><%=stu.age %></td>
                <td><%=stu.gender %></td>
                <td><%=stu.address %></td>
                <td>
                    <a onclick="return confirm('是否要删除学生<%=stu.name%>')" href="/students/delete?id=<%=stu.id%>">删除</a>
                    <a href="/students/to-update?id=<%=stu.id %>">修改</a>
                </td>
            </tr>
            <%}%>
        </tbody>
    </table>
    <%}else{%>
        <h2>没有学生信息</h2>
        <%}%>
    <hr>
        <form action="/students/add" method="post">
            <div><input type="hidden" name="crsf" value="<%=crsfToken %>"></div>
            <div>姓名：<input type="text" name="name"></div>
            <div>年龄：<input type="number" name="age" max="150" min="1"></div>
            <div>
                性别：<input type="radio" name="gender" value="男">男
                <input type="radio" name="gender" value="女">女
            </div>
            <div>地址：<input type="text" name="address"></div>
            <div>
                <button>添加</button>
            </div>
        </form>
</body>
</html>